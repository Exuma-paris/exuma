# Copy this file to exuma-secrets.env before sending the Windows installer.
# Do not commit the real exuma-secrets.env file.

UNTITLED_UI_TOKEN=replace-with-real-token

# Optional, only needed for image generation.
GEMINI_API_KEY=replace-with-real-key
